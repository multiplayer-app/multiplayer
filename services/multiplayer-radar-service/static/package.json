{
  "name": "agent-terminal",
  "version": "0.3.0",
  "scripts": {
    "build": "esbuild ./src/agent-terminal.js --bundle --outfile=./build/js/agent-terminal.js --platform=browser"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
